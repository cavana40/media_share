<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Likes

    <a data-toggle="collapse" href="#book_likes_new_form">
      Add like
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @book.likes.each do |like| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/likes/<%= like.id %>">
          <%= like.users_id %>
        </a>

        <%if current_user == like.users %>
          <a href="/delete_like_from_book/<%= like.id %>">
            Delete
          </a>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="book_likes_new_form">
        <%= render partial: "book_templates/book_likes_new_form" %>
      </div>
    </div>

  </ul>
</div>
